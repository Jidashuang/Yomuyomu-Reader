{
  "name": "yomuyomu-reader",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 backend/server.py --host 127.0.0.1 --port 8000",
    "test:backend": "python3 -m unittest discover -s tests/backend -p 'test_*.py'",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
